name: mango-jwt-ext
version: 1.0.0
description: extension for working with JWT tokens
deps:
   jphp-runtime: '*'

plugins:
   - Gradle

gradle:
   deps:
      - 'io.jsonwebtoken:jjwt:0.9.0'
      - 'javax.xml.bind:jaxb-api:2.3.1'
